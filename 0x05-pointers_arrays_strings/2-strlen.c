#include "main.h"

/**
  * _strlen - it computes the length of a given string
  *
  * @s: string whose length is to be computed for
  * Return: length of the string s
  */

int _strlen(char *s)
{
	return (strlen(s));
}
