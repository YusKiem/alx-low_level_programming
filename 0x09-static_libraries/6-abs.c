#include "main.h"
/**
  * _abs - returns an absolute value of an integer
  *@c:  integer beign tested
  * Return: Always 0
  */
int _abs(int c)
{
	if (c < 0)
		return (-c);
	else
		return (c);
	return (0);
}
